# Workspace-wide environment variables
# These are merged into .local.env in each repo during install
# Repo-specific overrides in repos/<name>.env take precedence
#
# Sensitive values have moved to Infisical (vault-backed via
# [env.secrets] in workspace.toml). Non-sensitive config stays here.
